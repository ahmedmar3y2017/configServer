server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/guest_service
    username: postgres
    password: postgres

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    url: jdbc:postgresql://localhost:5432/guest_service
    user: postgres
    password: postgres
